<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- API 31+ (Android 12+) draws its own system splash screen using these attributes,
         before `drawable/launch_background.xml` ever gets a chance to show — without this
         override the OS defaults to a plain white/black background with just the launcher
         icon, ignoring our brand color. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/launcher_icon</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
